# This file serves as a template for environment variables needed for the project.
# Please rename this file to .env and fill in the required values.

# Input and output directories
INPUT_AUDIO_DIR=path/to/input/audio
OUTPUT_AUDIO_DIR=path/to/output/audio
OUTPUT_TEXT_DIR=path/to/output/text
OUTPUT_HTML_DIR=path/to/output/html
OUTPUT_PDF_DIR=path/to/output/pdf

# Model configuration for transcription
MODEL_GPU=small  # or specify your preferred model
MODEL_CPU=tiny  # or specify your preferred model

# Other configurations
USE_GPU=True  # Set to False if you want to use CPU
```